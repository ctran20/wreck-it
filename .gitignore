# Visual Studio 2015 user specific files
wreck_it/.vs/

# Compiled Object files
wreck_it/*.slo
wreck_it/*.lo
wreck_it/*.o
wreck_it/*.obj

# Precompiled Headers
wreck_it/*.gch
wreck_it/*.pch

# Compiled Dynamic libraries
wreck_it/*.so
wreck_it/*.dylib
wreck_it/*.dll

# Fortran module files
wreck_it/*.mod

# Compiled Static libraries
wreck_it/*.lai
wreck_it/*.la
wreck_it/*.a
wreck_it/*.lib

# Executables
wreck_it/*.exe
wreck_it/*.out
wreck_it/*.app
wreck_it/*.ipa

# These project files can be generated by the engine
wreck_it/*.xcodeproj
wreck_it/*.xcworkspace
wreck_it/*.sln
wreck_it/*.suo
wreck_it/*.opensdf
wreck_it/*.sdf
wreck_it/*.VC.db
wreck_it/*.VC.opendb

# Precompiled Assets
wreck_it/SourceArt/**/*.png
wreck_it/SourceArt/**/*.tga

# Binary Files
wreck_it/Binaries/*
wreck_it/Plugins/*/Binaries/*

# Builds
wreck_it/Build/*

# Whitelist PakBlacklist-<BuildConfiguration>.txt files
!Build/*/
Build/*/**
!Build/*/PakBlacklist*.txt

# Don't ignore icon files in Build
!Build/**/*.ico

# Built data for maps
wreck_it/*_BuiltData.uasset

# Configuration files generated by the Editor
wreck_it/Saved/*

# Compiled source files for the engine to use
wreck_it/Intermediate/*
Plugins/*/Intermediate/*

# Cache files for the editor to use
DerivedDataCache/*
